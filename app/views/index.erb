Current links:
<ul>
  <% @last_ten_peeps.each_with_index do |peep, i| %>
    <%= "#{i+1}. #{User.first(id: peep.user_id).username} peeped: #{peep.peep_text} at #{peep.peep_timestamp.strftime("%H:%M on %A %B %d %Y")}" %>
  <% end %>
</ul>

Also link to see older peeps